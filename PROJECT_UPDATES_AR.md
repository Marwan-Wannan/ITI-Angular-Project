# تحديثات المشروع - متجر الأثاث Angular

## نظرة عامة
تم تحديث هذا المشروع لاستخدام **DummyJSON API** لجلب وإدارة منتجات الأثاث. تم إزالة مكون Observer لجعل المشروع أنظف وأكثر جاهزية للسيرة الذاتية.

---

## 🔄 التغييرات الرئيسية

### 1. **دمج API**
- **API القديم**: `http://localhost:3001`
- **API الجديد**: `https://dummyjson.com`

#### نقاط النهاية المستخدمة:
- **جلب كل الأثاث**: `GET https://dummyjson.com/products/category/furniture`
- **جلب منتج واحد**: `GET https://dummyjson.com/products/{id}`
- **إضافة منتج**: `POST https://dummyjson.com/products/add`
- **تحديث منتج**: `PUT https://dummyjson.com/products/{id}`
- **حذف منتج**: `DELETE https://dummyjson.com/products/{id}`

---

### 2. **المكونات المحذوفة**
- ❌ **مكون Observer** (`observar/`) - تم إزالته من:
  - المسارات (`app.routes.ts`)
  - شريط التنقل (`navbar.html`)
  - هذا يجعل المشروع أنظف لعرضه في السيرة الذاتية

---

### 3. **تحديث نموذج المنتج**
واجهة `IProduct` الآن تطابق هيكل DummyJSON API:

```typescript
export interface IProduct {
  id: number;
  title: string;              // تم التغيير من 'name'
  description?: string;
  category?: string;          // تم التغيير من 'categoryID'
  price: number;
  discountPercentage?: number;
  rating?: number;
  stock: number;              // تم التغيير من 'quantity'
  tags?: string[];
  brand?: string;
  sku?: string;
  weight?: number;
  dimensions?: {
    width: number;
    height: number;
    depth: number;
  };
  warrantyInformation?: string;
  shippingInformation?: string;
  availabilityStatus?: string;
  reviews?: any[];
  returnPolicy?: string;
  minimumOrderQuantity?: number;
  meta?: any;
  images?: string[];
  thumbnail: string;          // تم التغيير من 'PrdimgURL'
}
```

**التغييرات الرئيسية:**
- `name` → `title` (الاسم → العنوان)
- `quantity` → `stock` (الكمية → المخزون)
- `PrdimgURL` → `thumbnail` (رابط الصورة → الصورة المصغرة)
- `categoryID` → `category` (معرف الفئة → الفئة)
- `Material` → تم الإزالة (استُبدل بـ `brand` وخصائص أخرى)
- تمت إضافة العديد من الخصائص الاختيارية الجديدة لبيانات منتج أكثر ثراءً

---

### 4. **تحديث الخدمات**

#### `ProductService-With-API.service.ts`
- إضافة رؤوس HTTP المناسبة
- تحديث للتعامل مع تنسيق استجابة DummyJSON (يتضمن `products`, `total`, `skip`, `limit`)
- إضافة عامل `map` من RxJS لاستخراج مصفوفة المنتجات
- تحديث جميع الطرق للعمل مع هيكل API الجديد

```typescript
// مثال: جلب جميع منتجات الأثاث
getAllProducts(): Observable<IProduct[]> {
  return this.httpclient.get<DummyJsonResponse>(`${this.apiUrl}/category/furniture`)
    .pipe(map(response => response.products));
}

// مثال: إضافة منتج
addProduct(product: Partial<IProduct>): Observable<IProduct> {
  return this.httpclient.post<IProduct>(
    `${this.apiUrl}/add`, 
    JSON.stringify(product), 
    this.httpOptions
  );
}
```

---

### 5. **تحديث المكونات**

#### **مكون المنتجات** (`product.ts` & `product.html`)
- تحديث جميع روابط القالب:
  - `prd.name` → `prd.title`
  - `prd.quantity` → `prd.stock`
  - `prd.PrdimgURL` → `prd.thumbnail`
  - `prd.Material` → `prd.category`
- إضافة دعم لعرض نسبة الخصم من API
- تحديث فحوصات توفر المخزون

#### **مكون إدارة المنتج** (`admin-product.component.ts` & `.html`)
- تحديث عناصر التحكم في النموذج:
  - `name` → `title`
  - `quantity` → `stock`
  - `PrdimgURL` → `thumbnail`
  - `categoryID` → `category` (الآن حقل نصي، الافتراضي: 'furniture')
  - `Material` → `description` (حقل textarea)
- النموذج الآن يطابق متطلبات DummyJSON API
- رسائل النجاح تعرض معرف المنتج المُرجع

#### **مكون تفاصيل المنتج**
- تحديث جميع مراجع الخصائص لتطابق النموذج الجديد
- محسّن لعرض:
  - مصفوفة صور المنتج (أو الرجوع للصورة المصغرة)
  - الأبعاد (العرض، الارتفاع، العمق)
  - الوزن
  - معلومات الضمان
  - العلامة التجارية
  - نسبة الخصم
  - معلومات الشحن
  - سياسة الإرجاع
- تحديث عرض المنتجات ذات الصلة

---

### 6. **ملفات البيئة**
تم تحديث كل من `environment.ts` و `environment.development.ts`:

```typescript
export const environment = {
  production: false,
  BaseURL: 'https://dummyjson.com'
};
```

---

## 🚀 كيفية الاستخدام

### 1. **عرض المنتجات**
انتقل إلى `/Products` لرؤية جميع عناصر الأثاث من DummyJSON API

### 2. **إضافة منتج جديد**
- اذهب إلى `/admin/product`
- املأ النموذج:
  - **Title**: اسم المنتج (مثل "أريكة حديثة")
  - **Category**: "furniture" (افتراضي)
  - **Price**: سعر المنتج بالدولار
  - **Stock**: الكمية المتاحة
  - **Description**: وصف المنتج
  - **Thumbnail**: رابط صورة المنتج
- انقر على "Add Product"
- ⚠️ **ملاحظة**: DummyJSON هو API وهمي، لذا المنتجات لن تُحفظ بشكل دائم

### 3. **تعديل منتج**
- انقر على شارة "Edit" على أي بطاقة منتج
- عدّل التفاصيل
- انقر على "Update Product"

### 4. **حذف منتج**
- انقر على شارة "Delete" على أي بطاقة منتج
- أكد الحذف
- ⚠️ **ملاحظة**: هذا يحذف فقط من الحالة المحلية، وليس من API الفعلي

---

## 📝 مثال على استجابة API

### استجابة منتجات الأثاث:
```json
{
  "products": [
    {
      "id": 6,
      "title": "Wooden Dining Table",
      "description": "A beautiful wooden dining table",
      "category": "furniture",
      "price": 299.99,
      "discountPercentage": 5.5,
      "rating": 4.5,
      "stock": 12,
      "brand": "HomeStyle",
      "thumbnail": "https://cdn.dummyjson.com/products/6/thumbnail.png",
      "images": ["..."],
      "dimensions": {
        "width": 120,
        "height": 75,
        "depth": 80
      }
    }
  ],
  "total": 5,
  "skip": 0,
  "limit": 30
}
```

### استجابة إضافة منتج:
```json
{
  "id": 195,
  "title": "BMW Pencil",
  "category": "furniture",
  "price": 299,
  "stock": 50
}
```

---

## 🎯 ميزات جاهزة للسيرة الذاتية

✅ **دمج RESTful API** - يوضح استخدام HTTP client  
✅ **عمليات CRUD** - إنشاء، قراءة، تحديث، حذف المنتجات  
✅ **نماذج تفاعلية** - التحقق من صحة النماذج وإدارتها  
✅ **التوجيه** - تطبيق متعدد الصفحات مع التنقل  
✅ **تواصل المكونات** - تدفق البيانات بين الأب والابن  
✅ **RxJS Observables** - معالجة البيانات غير المتزامنة  
✅ **واجهة مستخدم حديثة** - Bootstrap 5 مع تصميم مخصص  
✅ **معالجة الأخطاء** - رسائل خطأ وتنبيهات مناسبة  
✅ **TypeScript** - كود آمن من حيث النوع  

---

## 🛠️ المجموعة التقنية

- **الإطار**: Angular 18+
- **التصميم**: Bootstrap 5 + SCSS
- **HTTP Client**: Angular HttpClient
- **إدارة الحالة**: RxJS Observables
- **النماذج**: Reactive Forms
- **التوجيه**: Angular Router
- **API**: DummyJSON (https://dummyjson.com)

---

## ⚠️ ملاحظات مهمة

1. **DummyJSON هو API وهمي**:
   - عمليات الإضافة/التحديث/الحذف تُرجع استجابات نجاح
   - لكن البيانات لا تُحفظ فعليًا على الخادم
   - مثالي لعروض الواجهة الأمامية والاختبار

2. **CORS**: DummyJSON API لديه CORS مفعّل، لذا لا حاجة لوكيل

3. **حد المعدل**: DummyJSON ليس لديه حدود معدل للاختبار

4. **معرفات المنتجات**: استخدم المعرفات الموجودة (1-194) لعرض المنتجات الحقيقية
   - فئة الأثاث لها معرفات محددة
   - المنتجات المضافة تحصل على معرفات وهمية (195+)

---

## 📚 المصادر

- **توثيق DummyJSON**: https://dummyjson.com/docs
- **منتجات الأثاث**: https://dummyjson.com/products/category/furniture
- **توثيق Angular**: https://angular.dev

---

## 🎓 نقاط التعلم

هذا المشروع يوضح:
- التكامل مع REST APIs الخارجية
- التعامل مع هياكل الاستجابة المختلفة
- واجهات TypeScript آمنة من حيث النوع
- إدارة دورة حياة المكون
- التحقق من صحة النموذج والإرسال
- معالجة الأخطاء وملاحظات المستخدم
- مبادئ التصميم المتجاوب
- تنظيم الكود النظيف

---

**تم التحديث**: نوفمبر 2025  
**API**: DummyJSON v1  
**إصدار Angular**: 18+
